<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.imbd">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IMBd">
        <activity android:name=".About"></activity>
        <activity android:name=".Statistics" />
        <activity android:name=".EditFilm" />
        <activity android:name=".SingleFilm" />
        <activity android:name=".FilmItem" />
        <activity android:name=".AddFilm" />
        <activity android:name=".WatchedFilms" />
        <activity
            android:name=".Home"
            android:label="Home" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>